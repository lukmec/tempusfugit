<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.lumdev.tempusfugit">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/my_icon8_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/my_icon8_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".settings.OLDSettingsActivity"
            android:label="@string/toolbar_settings_title" />
        <service android:name=".PermanentNotificationService" />

        <receiver android:name=".BootCompletedBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!--<receiver android:name="de.lumdev.tempusfugit.BootCompletedBroadcastReceiver" >-->
        <!--</receiver>-->

    </application>

</manifest>